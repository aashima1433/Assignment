let JWT,jwt;_13c‍.x([["default",()=>_13c‍.o]]);_13c‍.w("../config",[["JWT",["JWT"],function(v){JWT=v}]]);_13c‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
    static sign(payload, expiry = '1y', secret = JWT) {
        return jwt.sign(payload, secret, { expiresIn: expiry });
    }

    static verify(token, secret = JWT) {
        return jwt.verify(token, secret);
    }
}

_13c‍.d(JwtService);