let JwtService;_473‍.x([["default",()=>_473‍.o]]);_473‍.w("../services/jwtService",[["default",["JwtService"],function(v){JwtService=v}]]);

const auth = async (req, res, next) => {
    let authHeader = req.headers.authorization;
    if (!authHeader) {
        return next("unauthorized");
    }

    const token = authHeader.split(' ')[1];

    try {
        const { _id } = await JwtService.verify(token);
        const user = {
            _id
        }
        req.user = user;
        next();

    } catch(err) {
        return next(err);
    }

}

_473‍.d(auth);